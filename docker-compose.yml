version: "3"
services:
  web:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - redis
    environment:
      - REDIS_URL=redis://redis:6379
      - API_KEY=YOUR_API_KEY
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
